state Numeric initial:
  total: int = 9223372036854775808
  ratio: rational = 1/3
  invariant:
    total > 0 and ratio > 0 / 1

transition Accumulate @ Add(delta: int):
  from Numeric
  to Numeric:
    total = before.total * delta + 1
    ratio = before.ratio + 1 / 6
  where:
    delta > 0
